Smart Clipboard now shows a visible viewfinder button labelled **Clip** in the menu bar. This fixes an invalid symbol that left the previous button blank.

- Opening a second copy brings the existing app forward instead of registering competing shortcuts.
- Settings & Status shows screen permission, per-shortcut registration results, and the last received shortcut event.
- Shortcut configuration checks enabled macOS bindings and conflicting app registrations, preserves working keys when a replacement fails, and can find available alternatives.
- Capture opens setup when Screen Recording permission is missing.
- Opening the running app from Applications restores its panel.

Validation: 37 automated tests, including shortcut conflicts, safe replacement, independent registration and recovery; release build and signature verification. Screen access still requires your approval in macOS.

Apple Silicon, macOS 14 or newer. This preview is ad-hoc signed and is not Apple-notarized.
